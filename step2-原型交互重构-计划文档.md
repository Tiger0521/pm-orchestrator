# Step 2 原型设计与规范对齐 · 重构计划

> 目标：把 Step 2 从"输出静态 HTML + 旁挂一份交互说明文档"升级为"可交互原型 + 页面内联标注 + 局部框选迭代 + 参照现有 UI"，贴近墨刀/Figma 的真实工作方式。
> 本文只做构思与方案设计，不直接改 skill 文件。落地时按第六节顺序分步迭代。

---

## 一、问题：现在的 Step 2 离"可用原型"还差什么

当前 Step 2（`step2-原型设计与规范对齐/`）给用户三种原型方式：外网 Stitch、墨刀、交互式 HTML。其中"交互式 HTML"（方式 C）最接近诉求，但 `prototype-method.md` 第 4 节对它的描述只有一句"用 Write 工具直接生成自包含 HTML/CSS/JS 文件（含页面布局、基本交互、异常状态展示）"，没有可交互的标准、没有多页一致性约束、没有局部改稿、没有参照现有系统。而所有交互说明（布局、元素、异常、复用）都被拆到一份独立的 `<简称>-原型交互说明.md` 里，评审时要在原型和文档之间来回对照。

把你的五项诉求和现状逐条对一遍：

| 诉求 | 现状 | 差距 |
| ---- | ---- | ---- |
| 直接输出可交互 HTML 原型 | 方式 C 存在，但只说"基本交互"，无交互真跑标准、无多页一致性 | 方式 C 作为推荐主路径接上高保真交互生成能力（三方式并列保留、选择前置不省） |
| 页面内联标注（墨刀同款编号+引线） | 标注全部在独立的交互说明文档里，原型上无任何标注 | 需新增"标注层"覆盖物，把逻辑/校验/异常直接写在原型对应位置 |
| 框选局部修改 | 无机制，迭代等于整页重写 | 需定位到模块、只改选中区+其标注、其余 1:1 不动 |
| 智能区分大项目 & 小迭代 | `scale-adaptation.md` 只在项目级区分 new/iteration/refactor，Step 2 行只有"全部页面/聚焦新增页面" | 需在原型交付层加"变更规模"判断，小优化只更原型+标注不堆长文 |
| 参照现有系统 UI 迭代 | 无机制，`ui-style-presets/` 只服务于新设计 | 需能吃截图、提炼样式契约、按原系统风格出原型 |

一句话：**方式 C 的壳在，但内核空着；标注走错了地方（应在原型上而非独立文档）；迭代和参照两项完全没有。**

---

## 二、核心判断：不要重造，接上 rapid-prototype-craft

盘点现有能力时有一个关键发现：插件 `rapid-prototype-craft`（prototype-craft skill）的 intent 路由表已经覆盖了诉求里的三项——

- **new**（从零生成高保真交互原型）→ 对应诉求 1
- **iterate**（"只重写改动页/改动区，其余 1:1，不扩散"）→ 对应诉求 3
- **reference**（给定 URL/截图/现有 HTML，先提炼设计语言进契约，再实现）→ 对应诉求 5

它还自带保证一致性的"设计契约（Design Contract）"机制：技术栈+交付类型、design tokens、组件规格、App Shell+规范导航、页面清单、mock schema 一次性冻结，多页时最多 3 个子 agent 并行、每个只填主内容槽、导航逐字粘贴。这正是当前方式 C 缺的"多页不跑版"的根因解法。

所以方案不是从零造一个原型引擎，而是：

- **引擎复用**：把"交互式 HTML"的生成、局部迭代、参照还原三项，路由到 prototype-craft 的 new/iterate/reference 三个 intent；
- **设计契约吃现有存量**：prototype-craft 的 design tokens 字段直接消费 `ui-style-presets/` 里已有的预设，不另起一套风格系统；
- **唯一自建项**：页面内联标注层（诉求 2）——这是 prototype-craft 没有的，需要我们定义一套可切换的覆盖物规范；
- **打通落盘**：让带标注层的 HTML 成为 Step 2 的主交付物，沿用方式 C 的 Write 直写例外，标注数据直接取自现有 `proto-*.json` 字段，不新增数据结构。

这样改动面被压到最小：复用三项存量能力，只新增一个标注层文件，再修订几个现有文件的路由与落盘描述。

---

## 三、分项设计

### 3.1 可交互 HTML 原型作为推荐主路径

生成原型前必须让用户在三种方式里做一次选择，沿用现有 grilling 的三选一口径，不改为"默认直出、是否换 Stitch/墨刀"。交互式 HTML 作为推荐主路径接上 prototype-craft 的 new intent，按其 `② 设计契约 -> ③ 实现 -> ③.5 静态自检 -> ④ 交付` 流程跑；Stitch/墨刀保留为一等选项，本轮不动其详细执行步骤（留待后续设计），但选择口径和交互式 HTML 对等，不降级、不省略询问。

交互必须"真跑"，不是静态图。验收线直接搬 prototype-craft 的反 AI-slop 与交互清单：按钮可点、弹窗能开能关、筛选实时生效、表单有前端校验、列表有空/加载/错误三态切换、mock 数据 + stub API 不调真实后端。多页一致性由设计契约的 App Shell + 规范导航保证——这是当前方式 C 最大的缺口（多页时导航漂移、风格漂移），用契约的"导航逐字粘贴 + 只填主内容槽"一次性堵掉。

设计契约的 design tokens 字段从用户选的 `ui-style-presets/` 预设里取值（主色/中性色阶/字号/圆角/间距/阴影），prototype-craft 的 `references/design-spec.md` 第 1 节契约模板和我们的预设字段天然对得上，做一次字段映射即可，不动预设文件。

**选择提问设计**。三种方式并列给出，交互式 HTML 放第一位并标"推荐"，每种都解释清楚四件事：是什么、产出什么、适合什么场景、有什么门槛。推荐交互式 HTML 的理由一句话点明：无需外部账号和联网、交互真跑、原生支持内联标注+框选迭代+参照截图，主交付物即原型本身。字母重排为 A=交互式 HTML（需同步 `workflow.md` 第 4 节、`prototype-method.md` 第 1 节的 A/B/C 引用）。草拟提问模板：

```
原型生成方式选择（推荐 A）：

A. 交互式 HTML（推荐）-- AI 直接生成自包含 HTML/CSS/JS，浏览器打开即可点击交互（按钮/弹窗/筛选/表单/状态切换真跑），页面上直接打编号+引线标注，支持框选局部迭代、可参照现有系统截图对齐风格。无需外部账号、无需联网，主交付物即原型本身。
B. 外网 Stitch -- 通过 Google Stitch（AI UI 设计工具）从文字描述生成高保真 UI 设计稿，适合多视觉风格探索。需配置 Stitch MCP 与凭据；产出为设计稿截图/链接，交互弱于 HTML、标注需另写文档。
C. 墨刀 -- 通过墨刀在线原型工具创建可点击跳转的交互原型，适合团队在墨刀内协作评审。需墨刀专业版/企业版及令牌；产出为墨刀链接，迭代依赖墨刀平台、标注在墨刀内做。
补充描述：我自己填写
强制跳过：这个问题暂时不回答，记录为待验证并继续
```

提问时把推荐理由放在选项 A 的标签和开头一行，让用户一眼看到推荐项，但最终仍由用户拍板、不强选。

### 3.2 页面内联标注层（新增，唯一自建项）

这是五项诉求里 prototype-craft 不能直接覆盖的，需要新定义。机制如下：

**覆盖物而非侵入**。标注层是一层可切换的 overlay，挂在原型 HTML 之上，不改动原型本身的 DOM 与交互。两态切换：
- 预览态：干净原型，没有任何标注，用于演示、自测、给同事看；
- 标注态：显示编号标记 + 引线 + 标注卡，用于评审。

**编号与引线**。标注态下，原型里每个被登记的交互元素自动打编号（①②③…），用引线连到对应的标注卡。元素和标注的绑定靠 `data-ann` 属性：原型生成时给关键元素打 `data-ann="P1-E3"` 这类锚点，标注层据此定位编号和引线落点。

**标注卡内容**。每张卡对应一个元素，承载四类信息——功能逻辑（这个元素干嘛用）、交互规则（点了会发生什么、跳到哪）、校验规则（必填/格式/范围）、异常场景（空/加载/错误态文案）。这四类正好是现有 `proto-*.json` 里"元素说明表 + 异常状态"字段已有的内容，所以标注数据直接从 proto JSON 喂给标注层，不重新设计数据结构。

**主标注面易位**。标注层上线后，它就是 Step 2 的主标注面，原型即文档。独立的 `<简称>-原型交互说明.md` 降为可选导出（从同一份 proto JSON 渲染出来，给需要离线文档的场景留个出口）。

落地形态建议新增一个 `annotation-overlay.md`（标注层规范），定义覆盖物的结构、`data-ann` 约定、编号/引线/标注卡的渲染规则、两态切换开关、与 proto JSON 的字段映射。prototype-craft 生成原型时按本规范给元素打锚点、挂 overlay 脚本。

### 3.3 框选局部修改

复用 prototype-craft 的 iterate intent。它的原话是"精准定位改动页，局部重写，其余页面 1:1，不扩散"。我们要补的是"框选定位"这一步，让它从"改整页"细化到"改页内某个模块"。

机制：用户指明要改的区域（按模块/区块名，或描述"订单列表的筛选区"），agent 在 HTML 里定位到该模块的 DOM 边界，只重写这块的 HTML + CSS + 它对应的标注卡，其余 DOM、样式、标注逐字不动。这依赖 3.1 的模块化页面结构——每个页面只填主内容槽、组件是离散块，所以"只改一块"在结构上是可行的。

标注层同步只更新被改模块的编号和标注卡，未改模块的编号连号都保持不变（用稳定锚点 ID 而非顺序号，避免改一处全页重排编号）。

这条诉求本质上是 iterate intent + 模块级定位精度，不需要新引擎，只需要在 `prototype-method.md` 里把"局部迭代"作为一条明确路由写清。

### 3.4 大项目 vs 小迭代的智能区分

`scale-adaptation.md` 第 2 节已经有 new/iteration/refactor 三档，但 Step 2 那行只写了"全部页面生成原型 / 聚焦新增页面生成原型 / 只产出受影响页面的修改设计"，粒度停在"页面级"。诉求要的是更细的"变更规模"判断，落到单次改动的产出深浅上。

加一个 `change-scope` 信号，和 `projectType` 配合：

| 信号 | 判定 | 产出深浅 |
| ---- | ---- | ---- |
| 新页面 / 完整新模块 | 全新页面或独立模块从零做 | 完整设计契约 + office-hours"问题→方案对比→推荐→成功标准"结构 + 全套内联标注 |
| 日常小优化 | 加字段、改文案、调样式、挪位置 | 只更新原型 HTML + 对应标注卡，不重写流程说明、不堆长文 |

判定信号从上游 handoff 和用户本轮描述里读：用户说"加个手机号字段"就是小优化；说"做一个会员中心"就是新模块。`projectType=iteration` 时大多数 Step 2 轮次是小优化档，正好对应诉求里"清爽不啰嗦"。

这条不改架构，只把 `scale-adaptation.md` 第 2 节 Step 2 行和 `workflow.md` 的草案生成分支写细。

### 3.5 参照现有系统 UI 迭代

复用 prototype-craft 的 reference intent：给定截图/URL，先抓取或读图、把视觉语言提炼进设计契约、再跑实现。这一步天然产出"对齐原系统样式的新原型"，适合旧系统改版、不动 UI 风格。

和存量对接的点：reference intent 提炼出的 design tokens，可以落成一个自定义 `ui-style-preset` 文件（复用 `ui-style-presets/` 的文件格式：基本信息/设计 Token/字体配对/组件风格/动效规则/反 AI 味规则），存进预设目录。这样一次截图提炼，后续页面都能复用这套参照风格，不必每页重提。

`ui-design-style.md` 第 3.6 节已有"自定义风格"的口子（用户描述偏好就结合最近预设改 token），3.5 把它从"用户口述偏好"扩到"用户给截图、agent 提炼 token"，是同一个机制的增强，不是新机制。

---

## 四、落盘与产出契约的调整

标注层上线后，Step 2 的交付物主从关系变了，几个现有文件要跟着改：

- **主交付物**：带标注层的自包含 HTML（预览态/标注态同一份文件，靠开关切）成为主交付物，沿用方式 C 现有例外——用 Write 直写产品库 `详细设计/原型/` 目录，不走 JSON + `render-doc.sh`（见 `persist-guide.md` 第 3.1 节末段）。
- **标注数据来源**：标注层渲染所需的数据，就是现有 `proto-*.json` 的"元素说明 + 异常状态 + 组件复用"字段。不新增 JSON 结构，只让标注层脚本去读这些字段。
- **独立交互说明文档降级**：从"三类方式共有的伴生交付"降为"可选导出"。需要离线 md 的场景仍能从同一份 proto JSON 用 `render-doc.sh` 渲染出来，但不再是默认必交。
- **草稿数据块**：`output-contract.md` 第 4.2 节的"原型文档"数据块字段不变，"原型生成方式"取值仍为 `Stitch / 墨刀 / 交互式 HTML` 三选一、生成前必选；交互式 HTML 取值补充"含标注层"说明，Stitch/墨刀的取值口径不动，待后续设计。
- **模板**：`shared/templates/prototype.md` 的正文结构基本不动，"布局示意"一栏对 HTML 方式改为引用 HTML 文件路径 + 标注层开关说明。

这几处都是对现有契约的局部修订，不动六类文档的 ID 体系、frontmatter、追溯关系。

---

## 五、文件改造清单

按"复用存量、最小新增"的原则，改动集中在 step2 文件夹和两个 shared 文件：

| 文件 | 改动 | 要点 |
| ---- | ---- | ---- |
| `step2-原型设计与规范对齐/workflow.md` | 修订 | 原型方式 grilling 保持三选一、生成前必选（不改口径）；交互式 HTML 选定后路由 prototype-craft；草案生成分支按 change-scope 分大小档 |
| `step2-原型设计与规范对齐/prototype-method.md` | 大改 | 交互式 HTML 方式为主线接 prototype-craft（含标注层锚点）；第 1 节提问模板重排为 A=交互式 HTML 首位+标推荐+四要素解释（是什么/产出/适合/门槛），Stitch/墨刀保留为一等方式、详细执行步骤留待后续设计，不降级；新增"局部迭代"和"参照截图迭代"两条路由（仅作用于交互式 HTML 方式） |
| `step2-原型设计与规范对齐/annotation-overlay.md` | 新增 | 标注层规范：overlay 结构、`data-ann` 锚点约定、编号/引线/标注卡渲染、两态切换、与 proto JSON 字段映射 |
| `step2-原型设计与规范对齐/ui-design-style.md` | 小改 | 第 3.6 节"自定义风格"扩为支持"截图提炼 token→存成自定义预设"；补 design tokens 到契约字段的映射说明 |
| `shared/scale-adaptation.md` | 小改 | 第 2 节 Step 2 行细化：new=全部页面+完整契约；iteration 按变更规模分"新模块/小优化"两档产出深浅 |
| `shared/persist-guide.md` | 小改 | 第 3.1 节末段方式 C 例外补充"含标注层"；明确交互说明 md 降为可选导出 |
| `shared/output-contract.md` | 小改 | 第 1.3 节原型文档字段"原型生成方式"默认值改；标注交互说明 md 为可选 |
| `shared/templates/prototype.md` | 小改 | "布局示意"对 HTML 方式改为文件路径 + 标注层开关 |
| `detailed-design/instruction.md` | 小改 | 第 4.2 节动作前必读表，Step 2 原型设计前补读 `annotation-overlay.md`；第 4.3 节条件读补标注层文件 |

`ui-style-presets/` 不动，只新增一种"从截图提炼出的自定义预设"产生方式。

---

## 六、实施顺序

按"先打通主路径、再补标注、最后接迭代与参照"的顺序分步做，每步可独立验证，不批量改：

1. **主路径接通**：改 `prototype-method.md` 把方式 C 接到 prototype-craft 的 new intent，design tokens 吃 `ui-style-presets`；改 `workflow.md` 把方式 C 提为默认。验证：能出一份多页、导航不漂移、交互真跑的 HTML 原型。
2. **标注层**：新增 `annotation-overlay.md`，实现 overlay + `data-ann` + 编号引线 + 标注卡，数据从 proto JSON 喂。验证：原型上能切两态、标注卡四类信息齐全。同步改 `persist-guide`/`output-contract`/`templates` 把主交付物和可选文档关系调对。
3. **局部框选迭代**：在 `prototype-method.md` 补 iterate 路由 + 模块级定位。验证：改一个模块，其余 DOM 和标注逐字不动、编号不重排。
4. **大小档区分**：改 `scale-adaptation` 第 2 节 Step 2 行 + `workflow` 草案分支。验证：小优化轮次只出原型+标注、不堆长文。
5. **参照截图迭代**：扩 `ui-design-style.md` 第 3.6 节支持截图提炼、存自定义预设。验证：给一张旧系统截图，能按原风格出新页面原型。

---

## 七、需对齐与存疑的点

落地前有几点要先确认，避免边做边返工：

- **prototype-craft 能否被详细设计 subagent 调用**。详细设计是 pm-orchestrator 委派的 subagent（`agents/detailed-design-designer.md`）。要确认这个 subagent 的运行时带 Skill 工具、能加载 prototype-craft 插件 skill。若不带，要么改 subagent 的工具配置，要么由主调度器在委派前先加载 prototype-craft 再传上下文。这点决定路由方式，要先验证。
- **标注层 overlay 的实现形态**。是写一段可复用的 overlay JS/CSS（prototype-craft 生成原型时挂进去），还是让 prototype-craft 在生成阶段就把标注 DOM 一起渲染。前者解耦但要多挂一层脚本，后者耦合但渲染更稳。建议先用前者（overlay 脚本独立、原型 DOM 不被标注污染），如不稳定再退后者。
- **Stitch/墨刀的详细执行步骤留待后续设计**。已定：两种方式保留为一等选项，生成原型前的三选一为必选口径、不降级、不省略询问；本轮只先把交互式 HTML 接 prototype-craft 打通，Stitch/墨刀的详细执行步骤（MCP 调用、Prompt 增强、连接方式等）后续单独设计，不影响本轮主路径推进。
- **change-scope 信号的来源**。是让 subagent 从上游 handoff 自动推断，还是在 Step 2 grilling 里加一轮确认。自动推断省事但可能误判（把"重构筛选区"误判成小优化），加一轮更准但多一问。倾向自动推断 + 推断为小优化时在草案里显式标注"按小优化处理，如需完整流程说明请说明"，把纠错权交给用户。
