# Codex Memory

## Editing Principle

When updating this repository's skills or prompts, prefer changing the source definition of a concept, file, or workflow instead of adding separate downstream hard rules. File responsibilities, field meanings, and process contracts should be stated where they are first defined. Avoid duplicating the same constraint later unless a downstream component needs a local, role-specific instruction.